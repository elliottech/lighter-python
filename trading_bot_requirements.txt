# Requirements for the Lighter Random Trading Bot
# Install with: pip install -r trading_bot_requirements.txt

# Core SDK requirements
python_dateutil >= 2.5.3
setuptools >= 21.0.0
urllib3 >= 1.25.3, < 2.1.0
pydantic >= 2
typing-extensions >= 4.7.1
aiohttp >= 3.0.0
aiohttp-retry >= 2.8.3
websockets >= 12.0.0
eth-account >= 0.13.4
requests >= 2.31.0

# Additional dependencies for the trading bot
asyncio-throttle >= 1.0.2  # For rate limiting if needed
colorlog >= 6.7.0  # For colored logging (optional)
python-dotenv >= 1.0.0  # For environment variable management
